# build-log.md — Build Log (append-only)

**Project:** _TBD_   **Maintained by:** Claude Code (append-only)

> A chronological record of what was actually done each session/phase — a log, not a document. Never
> edit past entries; only append. High forensic value when something breaks ("what changed before
> this?").

🟦 **FOR CLAUDE CODE** — at the end of each session, append a dated entry: what was built/changed,
which phase, key decisions made, what broke and how it was fixed, and what's next. This complements
`todo.md` (state) by preserving history (sequence).

## Log
### _date_ — Phase _N_
- Built / changed:
- Decisions:
- Issues hit / fixed:
- Audit (Section 9): model used, conflicts found → logged to techdebt
- Next:
